services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:latest
    ports:
      - "127.0.0.1:6379:6379"
    restart: always